# Scenario Design and Implementation

> **Moved.** This page is superseded by
> [`docs/reference/scenario.md`](reference/scenario.md), which is kept current as the
> model evolves. Update your links/bookmarks; this stub exists only so old
> inbound links don't 404.
